# Moltbook Analytics API Configuration

# x402 Payment Settings (for paid tier)
X402_WALLET_ADDRESS=0xYourWalletAddressHere
X402_NETWORK=eip155:8453  # Base mainnet
X402_PRICE_PER_QUERY=0.01  # $0.01 per query
X402_MONTHLY_PRICE=2.00  # $2/month unlimited

# Rate Limiting
FREE_TIER_QUERIES=100  # First 100 queries free
FREE_TIER_RATE_LIMIT=10  # 10 queries/hour for free tier

# Cache
CACHE_TTL_MINUTES=5  # Cache profile data for 5 minutes

# Environment
ENVIRONMENT=production  # production | development
